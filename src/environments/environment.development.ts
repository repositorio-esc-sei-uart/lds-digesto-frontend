export const environment = {
  /**
   * Indica si la aplicación está corriendo en modo producción.
   * Déjalo en 'false' para desarrollo.
   */
  production: false,

  /**
   * La URL base de tu API backend cuando corres localmente.
   * Asegúrate de que tu backend Spring Boot esté corriendo en este puerto.
   */
  apiUrl: 'http://localhost:8080'
};

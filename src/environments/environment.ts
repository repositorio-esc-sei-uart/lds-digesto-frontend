export const environment = {
  /**
   * Indica que SÍ estás en modo producción.
   */
  production: true,

  /**
   * La URL base de tu API backend cuando esté desplegada en el servidor de producción.
   * 🚨 ¡IMPORTANTE! Reemplaza esta URL de ejemplo con la URL real de tu backend
   * cuando lo despliegues.
   */
  apiUrl: 'https://tu-api-produccion.unpa.edu.ar'
};
